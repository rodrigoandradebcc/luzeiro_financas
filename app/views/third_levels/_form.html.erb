<%= form_for(@third_level) do |f| %>
  <% if @third_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@third_level.errors.count, "error") %> prohibited this third_level from being saved:</h2>

      <ul>
      <% @third_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :second_level_id %><br>
    <%= f.text_field :second_level_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
